{
  "name": "elm-parcel-hello-world",
  "version": "1.0.0",
  "description": "Steps to run the project:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url='./'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm-hot": "^1.1.5",
    "node-elm-compiler": "^5.0.5"
  },
  "comments": {
    "devDependencies": {
      "parcel-bundler": "Could add this as a local dependency by running `npm install parcel-bundler --save-dev`; the only reason I haven't is to save local disk space."
    }
  }
}
